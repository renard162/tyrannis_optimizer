[project]
name = "tyrannis-optimizer"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Samuel Oliveira", email = "renard162@gmail.com" }
]
requires-python = ">=3.10"
dependencies = [
    "pyspark==4.1.3",
]

[project.scripts]
tyrannis-optimizer = "tyrannis_optimizer:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
